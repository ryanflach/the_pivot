  <div class="page-content">
  <h1>All Items</h1>
  <%= render partial: "filter" %>

  <%= render partial: 'shared/item_list', locals: { btn_label: 'Add to Cart', btn_class: 'default', status: false} %>
  <% if current_admin? %>
    <div class="col-md-12">
      <%= link_to 'Add New Treasure', new_admin_item_path, class: 'col-md-2 col-md-offset-10 btn btn-primary' %>
    </div>
  <% end %>
</div>
